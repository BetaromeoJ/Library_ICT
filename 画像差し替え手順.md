# 画像差し替え手順

このサイトの画像は、すべて「同じファイル名で上書きする」だけで差し替えができるように作られています。HTMLファイルを編集する必要はありません。

---

## 1. 画像ファイルの保存場所

すべての画像は、次のフォルダに入っています。

```
school-library-ict-portal/assets/images/
```

| ファイル名 | 用途 | 推奨サイズ(横×縦) | 推奨の縦横比 | 形式 |
|---|---|---|---|---|
| `profile.jpg` | 講師プロフィール写真 | 600×600px 以上 | 正方形(1:1) | JPG または PNG |
| `book01.jpg` | 著作1冊目の書影 | 600×850px 程度 | 縦長(約3:4.2) | JPG または PNG |
| `book02.jpg` | 著作2冊目の書影 | 600×850px 程度 | 縦長(約3:4.2) | JPG または PNG |
| `hero-library.jpg` | トップページの背景イメージ(任意) | 1600×900px 以上 | 横長(16:9程度) | JPG |

QRコード画像を追加する場合は `assets/images/placeholders/` フォルダに保存すると管理しやすくなります(例: `slido-qr.png`)。

---

## 2. 差し替えの手順(HTMLを変更しない方法)

1. 差し替えたい画像を用意する
2. 上の表と**まったく同じファイル名**に変更する(例: 新しいプロフィール写真も `profile.jpg` という名前にする)
3. `assets/images/` フォルダの中にある古いファイルを、新しいファイルで上書き保存する
4. ブラウザでサイトを開き直す(表示が変わらない場合は、ブラウザの再読み込み・キャッシュ削除を試してください)

ファイル名を変える場合(例: `profile.png` にしたい場合)は、該当するHTMLファイル内の `src="assets/images/profile.jpg"` の部分も同じ名前に書き換える必要があります。

---

## 3. プロフィール写真の差し替え

- 場所: `assets/images/profile.jpg`
- 使われているページ: `index.html`(講師紹介セクション。動画の読み込み中や再生に対応していないブラウザでは、この画像がそのまま表示されます)
- 正方形に近い写真がきれいに表示されます。顔がまんなかに来るように調整してください。
- 画像がない場合は、自動的に人物アイコンと「プロフィール写真」という文字のプレースホルダーが表示されます。

### プロフィール動画(講師紹介を動く表示にする場合)

- 場所: `assets/videos/instructor.mp4`
- 正方形(560×560px程度)、6秒前後のループ動画を推奨します。ファイルサイズは1MB以内が目安です。
- 音声は自動的にミュートされて再生されます。
- 差し替える場合は、同じファイル名(`instructor.mp4`)で上書きしてください。HTMLの変更は不要です。
- 動画が再生できない場合や、訪問者が「視差効果を減らす(reduce motion)」を設定している場合は、自動的に `profile.jpg` の静止画に切り替わります。

---

## 4. 書影(書籍の表紙)の差し替え

- 場所: `assets/images/book01.jpg`, `assets/images/book02.jpg`
- 使われているページ: `index.html`(著作・関連書籍セクション)
- 縦長の表紙画像がそのまま使えます。
- 画像がない場合は、自動的に色付きのプレースホルダー(「書影を準備中です」)が表示されます。
- 3冊目以降を追加する場合は、`book03.jpg` のように新しいファイルを保存し、README.md「書籍画像と情報の差し替え方法」の手順でHTMLにカードを追加してください。

---

## 5. Hero画像(トップページの背景)の差し替え

トップページの背景は、画像がなくても綺麗に表示されるCSSデザイン(グラデーション)になっています。写真を使いたい場合は、以下の手順で追加できます。

1. `assets/images/hero-library.jpg` を、使いたい写真で上書きする
2. `index.html` の `<section class="hero">` の直後に、次のようなコードを追加する(未追加の場合の一例です)

```html
<img src="../assets/images/hero-library.jpg" alt="" aria-hidden="true"
     style="position:absolute; inset:0; width:100%; height:100%; object-fit:cover; opacity:.18; z-index:-1;"
     onerror="this.remove()">
```

画像が用意できない場合は何もしなくて構いません(このファイルはプレースホルダー画像として同梱されています)。

---

## 6. QRコードの差し替え

- 場所: `courses/course03-slido.html` 内、「Slidoに参加する」セクション
- 現在は `【ここにSlidoのQRコードを配置】` という文字のプレースホルダーが表示されています
- QRコード画像を用意し、`assets/images/placeholders/` フォルダに保存したうえで、該当箇所を次のように書き換えてください。

```html
<div class="video-frame" style="padding-top:100%; max-width:260px;">
  <img src="../assets/images/placeholders/slido-qr.png" alt="Slido参加用QRコード"
       style="position:absolute; inset:0; width:100%; height:100%; object-fit:contain;">
</div>
```

---

## 7. 画像が表示されない場合の確認方法

1. **ファイル名を確認する** — 大文字・小文字、拡張子(`.jpg` / `.jpeg` / `.png`)が完全に一致しているか確認してください。
2. **保存場所を確認する** — `assets/images/` フォルダの直下に置かれているか確認してください(サブフォルダに入れると表示されません)。
3. **ブラウザのキャッシュを確認する** — 画像を差し替えた直後は、ブラウザに古い画像が残っていることがあります。再読み込み(Windows: Ctrl+F5 / Mac: Cmd+Shift+R)を試してください。
4. **ファイルが壊れていないか確認する** — 画像ファイルを直接ダブルクリックして開けるか確認してください。
5. **ローカルサーバーで確認する** — `file://` で直接開くとブラウザによっては表示が不安定になることがあります。README.md「3. ローカルで確認する方法」を参考に、簡易サーバー経由で確認してください。

---

© 2026 Michihiro Wada. All Rights Reserved.
